class Solution {
    public static String reverseString(String s) {
        // code here
        String r = "";
        int n= s.length();
        for(int i=n-1; i>=0; i--){
            r= r+ s.charAt(i);
        }
        return r;
    }
}
